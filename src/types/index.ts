export type User = {
    id: string;
    name: string;
    age: number;
    isStudent: boolean;
}

export type Message={
   photoURL: string ,
   userId:string,
   message:string,
   id:string,
   time:string,
   userName:string,
}